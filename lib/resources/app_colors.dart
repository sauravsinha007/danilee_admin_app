import 'package:flutter/material.dart';

/// Declared as static constants 'abstract' classes
/// to restrict creation of constructors of these classes

abstract class AppUIColors {
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF8E8E93);
  static const Color green = Color(0xFFA6CE39);
}
